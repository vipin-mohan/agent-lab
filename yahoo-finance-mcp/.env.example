# Yahoo Finance MCP Server — Environment Variables
# Copy this file to .env and adjust as needed.

# Port the server listens on (default: 8000)
PORT=8000

# Host to bind to (default: 0.0.0.0 for all interfaces)
HOST=0.0.0.0
