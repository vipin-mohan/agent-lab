# agent-lab

## Task API

The FastAPI task list app lives in **task-api/**.

### Run locally

```bash
cd task-api
# Optional: create and activate venv, then install deps
pip install -r requirements.txt
uvicorn main:app --reload
```

Open http://127.0.0.1:8000/
